# ========================================
# Cloudflare-Memos 后端 API 服务配置
# ========================================

name = "memos-api"
main = "src/index.js"
compatibility_date = "2024-01-15"
compatibility_flags = ["nodejs_compat"]

[vars]
# 时区配置（IANA 时区数据库格式）
# 示例: "Asia/Shanghai", "America/New_York", "Europe/London"
# 留空则使用 UTC
TIMEZONE = "Asia/Shanghai"

# JWT_SECRET 不要写在这里。
# 本地开发请放到 backend/.dev.vars
# 部署环境请使用: wrangler secret put JWT_SECRET

# ========================================
# 数据库配置
# ========================================
[[d1_databases]]
binding = "DB"
database_name = "memos_db"
database_id = "e03f026e-1974-4b76-844a-e09b4f08ae08"

# ========================================
# 对象存储配置
# ========================================
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "memos"
